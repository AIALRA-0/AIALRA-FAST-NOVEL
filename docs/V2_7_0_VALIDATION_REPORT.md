# 小说证据图谱 2.7.0 验证报告

验收日期：2026-08-27

## 结论

2.7.0 已完成本机实现与固定样本验收

VPS 已完成 2.7.0 更新和回滚点保留

GitHub 已保留公开历史并发布 2.7.0，精确公开副本安全门禁和自动质量检查均已通过

公网域名仍受 Cloudflare 最小权限令牌阻断，不能标记为公网完成

## 实现结果

| 项目 | 验收结果 |
|---|---|
| 统一界面 | 黑白应用壳层，人物、地点、势力、路线和状态使用统一语义色 |
| 语义世界图 | 稳定拓扑、方向硬约束、语义区域、标签避让和随地点数量扩展的世界坐标 |
| 双视图 | 2D 与 3D 共用 X/Y、步骤、人物、地点、播放状态和原文入口 |
| 三维纵深 | Z 只来自有证据的包含层级，不随机制造地理信息 |
| 叙事记忆 | 保存前因、行动、结果、状态变化、未闭合线索和因果引用 |
| 知识库 | 原子事实、概念体系和读者视图三层结构，包含检索、分类、编辑和修改记录 |
| 成本 | 新增叙事展示由本地编排器生成，地图与知识派生视图不调用模型 |
| Windows 成品 | 2.7.0 压缩包构建成功，隔离数据库启动后 `/readyz` 返回 2.7.0 |

## 自动验证

- Python：104 项通过，1 项按环境条件跳过
- Chromium 合成数据：3 条主流程通过
- Chromium 真实长篇：1 条《西游记》流程通过
- JavaScript 语法：通过
- 当前状态合同：通过
- README 双语、安全模式、链接和图片元数据：0 错误、0 警告

## 真实长篇边界

验收副本包含 203 个章节、1049 个故事编年步骤和 94 个当前相关地图地点

验证覆盖全部节点保留、默认标签无重叠、2D/3D 步骤一致、末步结束和浏览器控制台无错误

验收数据库与真实小说截图只保留在本机并受版本排除规则保护

本报告不能推出任意小说达到 95% 准确率

正式内容质量仍需至少五部真实作品、300 条人工金标准、关键案例 100% 通过和保留集加权准确率不低于 95%

## VPS 验收

- 当前镜像为 2.7.0，容器健康状态正常
- `/readyz` 返回 `ready / 2.7.0`
- 容器使用 `10001:10001`，根文件系统只读
- 服务只绑定 VPS 回环地址，公网不能绕过 Nginx 直连应用端口
- 公开数据卷只含四本合成演示书
- Nginx 配置检查通过
- 手动解析主机名时，匿名访问返回 Authentik 登录跳转
- DNS 仍不存在，现有证书也不匹配目标主机名，因此公网验收没有通过

## 未完成项

- `novel.aialra.online` 的 DNS、正确主机名证书和 Authentik 公网往返验收
