# 当前交付状态

当前发布目标是 `2.9.2-rc.1` 候选版，正式质量门仍由 300 条人工金标准和 60 条有效密封保留案例阻断

## 1 本轮实现

- 地图区域随 `StoryMapStep` 自动联动，当前区域高对比显示，上级区域中等显示，其他区域保留淡化轮廓
- 地点未知时不猜测新区域，只把上一次确认区域标成上次确认
- 区域主名称来自真实地点或原文容器，不再出现编号式故事拓扑片区
- 区域标签移到边界外侧，使用短连接线指回区域，并纳入全图边界计算
- 2D 与 3D 使用相同区域名称、成员、状态和步骤，3D 当前板块提高透明度差异
- 播放支持 `0.5×`、`1×`、`1.5×`、`2×`，速度变化不重置步骤并保存到本机浏览器
- 防剧透进度删除可见编辑按钮，保留双击、Enter、空格、Escape 和失焦保存
- 书库增加来源、语言、许可、真实或虚构标记、导入时间与实际分析范围
- 私有真实语料扩展为 12 部开放作品，合成内容进入功能演示目录
- 地图布局升级为 `semantic-atlas-v2.9.2-lod12`，无关区域按最外层家族分离，父子区域递归嵌套
- 地点之间保留至少约 118 个布局单位，区域标签和地点标签获得稳定留白
- 区域边界使用不随缩放变细的描边，非当前区域也保留清晰轮廓和底色

## 2 当前验证证据

- 十二部全文已通过真实下载、格式检查、分章、正文哈希和唯一原文锚点检查
- 文本格式覆盖 TXT、HTML 和 EPUB，语言覆盖中文、英文和日文
- 全部作品共 1,868 个原文片段，正文规模超过 619 万字符
- 十二部作品已逐本通过三栏书库搜索、打开、来源和分析范围浏览器验收
- 120 章压力演示的四个区域均使用真实地点组合命名，步骤切换后当前区域同步变化
- Python 自动测试为 137 项通过、1 项跳过，跳过项只涉及 Windows DPAPI 受保护路径
- JavaScript 语法检查通过
- 本机 16 本作品地图回放结果为无关区域碰撞 0 组、父子层级未包住 0 组、最近地点间距下限约 118
- Playwright 已在《西游记》的 1440×900 与 720×960 实际页面验证 12 个区域完整渲染、区域描边不随缩放变细、页面无整体横向溢出和控制台错误
- 本机最终数据库浏览器流程为 9 项通过、2 项按条件跳过
- VPS 真实延迟浏览器流程为 9 项通过、2 项按条件跳过
- GitHub 公开候选版质量门通过，包含 Python、JavaScript、8 项公开浏览器回归和容器构建，3 项私有真实语料流程按条件跳过
- 知识库切书竞态已用 450 毫秒接口延迟稳定复现并修复，右栏不再混入上一本书的概念
- 十二部真实开放作品已完成 142 个首批片段分析，失败任务 0 个
- 真实开放作品当前保存 6,146 条证据连接，原文逐字核验缺失 0 条
- Luna 本地登录通道共完成 178 次调用，输入 14,622,905 标记、输出 770,851 标记，这属于 ChatGPT 计划用量，不虚构美元费用

## 3 真实质量口径

金标准是人工看过原文后确认的测试答案，机器生成和自动通过都不能代替人工确认

正式质量集为 12 部作品，每部 25 条，其中 20 条开发案例和 5 条密封保留案例，总计 300 条金标准与 60 条密封案例

- 直接覆盖作品为《西游记》《红楼梦》《水浒传》《三国演义》《聊斋志异》
- 开放代理样本为《镜花缘》《海上花列传》、`Pride and Prejudice`、`The Adventures of Sherlock Holmes`、`A Princess of Mars`、`銀河鉄道の夜`、`The Spiraling Web`
- 开放代理样本不能冒充现代商业网文、商业轻小说、现代女频或现代都市小说的直接验证
- 当前机器候选 28 条，可证明的人工确认 0 条，有效密封保留案例 0 条

因此候选版可以报告跨语言、跨文体导入与交互覆盖，不能显示达到 95% 通用小说准确率

## 4 发布状态

- 本机候选为 `2.9.2-rc.1`
- VPS 已运行 `2.9.2-rc.1`，发布基线为 `a49d1ab`，界面竞态修复基线为 `d2c3a05`
- 公网根页面和 `/api/books` 均会把未登录访问重定向到 Authentik
- 登录后完整往返需要用户现有 Authentik 会话完成最终人工验收，自动化没有读取用户凭据
- 发布数据卷备份位于 VPS 的 `20260829T033921Z-pre-final-2.9.2-rc.1` 目录，上一发布目录和镜像仍保留
- GitHub 候选版由 `v2.9.2-rc.1` 标签和同名 Release 固定，公开质量门状态为通过
- Cloudflare 不读取或使用对话中曾暴露的高权限令牌
- 该旧令牌仍需用户在 Cloudflare 控制台手动撤销，当前令牌没有管理 API Token 自身的权限

运行 `scripts/check_current_state.ps1` 可以核对版本与双视图合同
