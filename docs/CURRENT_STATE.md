# 当前交付状态

当前发布目标是 `2.9.4-rc.1` 候选版；正式质量门仍由 300 条人工金标准和 60 条有效密封保留案例阻断

## 1 本轮已经实现

- 地图区域改用 Shapely 几何内核；边界同时计算节点圆形、节点文字、必要连接廊道和子区域占用范围
- 每个区域保存包含检查结果和失败原因；紧凑几何无法通过时退回保证包含的凸包
- 嵌套标签优先进入内部标题角；空间不足时进入左右标签轨道并使用短连接线指回区域
- 三维区域由上下表面和侧壁组成封闭外壳；父区域完整包住子区域体积；提供外壳、剖面和当前层观察方式
- 编年、二维地图、三维地图、播放条、详情和原文跳转继续只读取 `StoryMapStep`
- 分离故事增加 `NarrativeUnit` 与 `StoryWorld` 软分区；用户可以改名、调边界、移动、拆分、合并或恢复系统建议；一本书不会被拆成多个书库项目
- 人物关系、编年、地图、世界信息和知识库可以按整本书、当前世界或当前故事筛选
- 增量更新使用章节哈希和片段指纹识别追加、插入、修改、删除、改名和重排；未变化章节不产生模型调用
- 修改或删除章节只使相关证据和模型事实失效；人工编辑保留；章节重排后重新校准正式事实的首次证据位置
- 身份、关系、地点和时间问题统一进入待处理事项；每项显示问题、影响、建议、证据、推荐操作和重建范围
- 自动解决且没有证据缺口的记录移入历史；待处理关系不再只是日志；用户可以自动复核全部、逐项处理或保持独立
- 人物、事件、关系、地点关系、世界信息和条目开放语义字段编辑；内部 ID、哈希和逐字证据坐标保持只读
- 左侧固定文件夹与书籍树支持搜索、定位和展开状态记忆；移动端使用独立抽屉
- 三维关系图的标签不再每帧销毁；悬停节点或关系会稳定高亮并更新关系说明
- 地图右栏使用单一滚动容器；1366×768 首屏可直接看到当前标题、摘要和原文入口；其余事实和编年可以连续下探
- 系统界面和本地生成摘要采用中文分号风格；逐字原文、用户输入和导入文本保持原样
- 质量检查增加人工验收中心；直接解释金标准与密封题，并提供打开原文、接受、修正、密封和第二遍复核入口
- 每本书可以选择跟随原文、中文或 English；选项进入完整提示词和运行哈希，只影响后续生成的读者报告，不改写逐字证据
- 二维与三维地点、区域标签支持按实际文字宽度自动换行；区域边界同步纳入多行标签占用范围
- 三维地图与三维关系图的方向箭头放大；长标签保持屏幕内可读；世界知识概念创建表单在右栏恢复整齐单列

## 2 当前验证证据

- Python 自动测试为 149 项通过、1 项跳过；跳过项只涉及 Windows DPAPI 受保护路径
- JavaScript 语法与当前状态合同检查通过
- GitHub Actions `33271299183` 已通过 Python、浏览器和容器构建门禁
- Shapely 几何回归逐个验证区域包含节点、文字框和子区域；三维体积的 `z_min` 与 `z_max` 有效
- 增量回归覆盖追加、插入、修改、删除、改名和任意章节重排；人工修正不会因增量更新丢失
- Chromium 浏览器累计 11 项通过、3 项条件跳过；条件项需要本机扩展夹具，不用于替代当前真实作品验收
- 浏览器验证 5 种视口与 4 种有效缩放比例；地图工具栏无重叠；右栏能够到达最后内容；快速切换不会从错误地点重新出发
- 三维关系标签悬停、故事世界范围切换、统一选择器、防剧透输入、最后一步停止和弹窗焦点恢复均已通过
- 十二部开放作品逐本通过三栏书库搜索、打开、来源、许可和分析范围验收
- 十二部全文共 1,868 个原文片段、超过 619 万字符；覆盖中文、英文、日文、TXT、HTML 和 EPUB
- 现有私有验收库保存 142 个已分析真实片段、6,146 条逐字证据连接、0 条原文命中缺失和 0 个失败任务
- Luna 本地登录通道历史记录为 178 次调用、14,622,905 个输入标记和 770,851 个输出标记；这属于 ChatGPT 计划用量，不虚构美元费用

## 3 真实质量口径

金标准是人工看过原文后确认的测试答案；机器生成、页面能打开和自动规则通过都不能代替人工确认

正式质量集为 12 部作品；每部 25 条，其中 20 条开发案例和 5 条密封保留案例；总计 300 条金标准与 60 条密封案例

人工确认入口位于“质量检查 → 金标准 → 人工验收中心”；审核者先打开原文，确认系统题目和答案是否正确，再选择接受或修正；选择“正确并作为密封题”后，还需要第二位审核者复核，才计入有效密封案例

- 直接覆盖作品为《西游记》《红楼梦》《水浒传》《三国演义》《聊斋志异》
- 开放代理样本为《镜花缘》《海上花列传》、`Pride and Prejudice`、`The Adventures of Sherlock Holmes`、`A Princess of Mars`、`銀河鉄道の夜`、`The Spiraling Web`
- 开放代理样本不能冒充现代商业网文、商业轻小说、现代女频或现代都市小说的直接验证
- 当前机器候选 28 条；可证明的人工确认 0 条；有效密封保留案例 0 条

因此候选版可以报告跨语言、跨文体导入与交互覆盖；不能显示达到 95% 通用小说准确率

## 4 发布状态

- 本机候选为 `2.9.4-rc.1`；README 截图由 120 章合成压力演示重新生成，不含用户小说、账号、真实地址或模型密钥
- VPS 已切换到干净候选提交 `1955ba8`；运行镜像摘要为 `41f7b59c6eee`；就绪接口、最新前端竞态保护和数据卷挂载均已核对；旧发布目录和旧镜像继续保留
- GitHub 完整旧历史触发安全门禁；新版改用只有一条已审查根提交的干净候选分支发布；旧 `main` 不删除、不改写，可随时恢复为默认分支
- VPS 继续只监听回环地址；未登录根页面和书库接口由 Authentik 保护
- 服务器内部就绪检查与未登录访问边界已通过；根页面、书库接口和就绪接口均会先跳转 Authentik；用户现有 Authentik 登录会话已经核实能够进入应用与私有书库；自动化没有读取用户凭据
- Cloudflare 不读取或使用对话中曾暴露的高权限令牌；旧令牌仍需在 Cloudflare 控制台撤销

运行 `scripts/check_current_state.ps1` 可以核对版本与双视图合同
