# 当前交付状态

本文件是后续任务的第一入口

当前发布目标是 2.7.0，工作范围包括以下八项

1. 编年、二维地图和三维地图共用同一个 `StoryMapStep`
2. 二维和三维切换不改变当前步骤、播放状态、地点详情和原文入口
3. 页面使用黑白灰、清晰间距和可见键盘焦点，主要操作不小于 40 像素
4. 公开仓库不包含数据库、小说、密钥、日志、内部部署资料或真实用户截图
5. VPS 只运行空库或合成演示数据，并由 ExampleOrg 的 Authentik 统一登录保护
6. 语义世界图使用稳定拓扑和带证据的方位约束，彩色片区不作为正式地理事实
7. 叙事记忆保存前因、目标、行动、结果、状态和未闭合线索，所有非空事实都需要逐字证据
8. 知识库分为原子事实、概念体系和读者视图，并保留人工修改记录

## 不再重新讨论的结论

- 地图不是另一套剧情，地图只是编年步骤的空间投影
- 三维不是随机散点，X/Y 沿用二维方位，Z 只表示原文明示的包含层级
- 地点未知时人物标记隐藏，但该编年步骤仍然存在
- 用户快速点下一步时，人物从屏幕上的当前动画帧继续移动
- 公开部署不能复制本机正式数据库，也不能内置模型密钥

## 当前门禁

机器可读状态见 `docs/current-state.json`

2.7.0 本机门禁已经完成 104 项 Python 测试、3 条合成数据浏览器流程、1 条《西游记》长篇浏览器流程、JavaScript 语法检查、状态合同检查和 Windows 成品启动检查

真实长篇验收覆盖 203 个原文章节、1049 个编年步骤和 94 个当前相关地点，2D 与 3D 保持同一步骤，默认标签没有重叠，末步不会永久转圈

这些结果只证明固定样本，不表示任意小说天然达到 95% 准确率

VPS 已切换到 2.7.0，容器使用非 root 用户、只读根文件系统并只绑定回环地址

更新前数据库备份保留在服务器仓库外，`current` 指针已经指向实现提交 `35afa46`

使用手动主机解析时，匿名请求会被 Authentik 网关以 302 跳转到登录页

当前剩余外部阻断项是 Cloudflare 最小权限令牌和最终 GitHub 推送

浏览器存在账户登录线索，但浏览器控制插件的本地链接失效，命令行也没有登录态或令牌

Wrangler OAuth 会申请明显超过 `Zone Read + DNS Edit` 的权限，因此没有授权

获得只限定 `aialra.online` 的令牌后，才能创建 DNS、签发精确域名证书，并完成公网匿名跳转与登录后访问验收

在此之前不得对外宣称公网发布完成

运行 `scripts/check_current_state.ps1` 可以检查版本一致性和双视图是否仍然共用编年入口
