# 项目执行约束

每次开始任务先读 `docs/CURRENT_STATE.md`、`docs/current-state.json` 和 `docs/PRODUCT_CONTRACT.md`，再运行 `scripts/check_current_state.ps1`。聊天摘要只能提供线索，不能代替仓库里的当前状态。

实现过程中遵守以下固定合同：

- 剧情编年、二维地图、三维地图、播放条和原文跳转必须读取同一个 `StoryMapStep` 序列。
- 二维地图负责呈现有证据的方位；三维地图沿用同一组二维坐标，纵深只能表示有证据的包含层级，不能随机制造地理信息。
- 切换二维和三维时保留当前步骤、人物、地点、播放状态和原文入口，不调用模型，也不生成第二套事实。
- 所有正式事实必须能回到原文。证据不足时显示未知或进入可解决冲突，不能猜测补全。
- 版本、自动测试、浏览器验收、公开仓库和部署状态发生变化后，同步更新 `docs/current-state.json` 与 `docs/CURRENT_STATE.md`。
- 发布前必须使用仓库外的干净副本执行安全门禁；数据库、上传小说、密钥、日志、构建目录和真实用户截图不得进入公开仓库。
